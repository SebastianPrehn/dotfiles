;;; if-hl-todo.el --- Setup hl-todo -*- lexical-binding: t -*-
;;; Commentary:

;; Visually highlight TODO's in files.

;;; Code:

(use-package hl-todo
  :init
  (global-hl-todo-mode))

(provide 'if-hl-todo)
;;; if-hl-todo.el ends here
