;;; dev-undo-tree.el --- Configuration of Undo-tree -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(use-package undo-tree
  :config
  (global-undo-tree-mode))

(provide 'dev-undo-tree)
;;; dev-undo-tree.el ends here
